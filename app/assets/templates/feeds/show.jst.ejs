<ul>
<% entries.each( function(entry) { %>
  <li>
    <a href="<%= entry.escape("link") %>">
      <%= entry.escape("title") %>
    </a>
  </li>
  <% }); %>
</ul>
<button class="refresh">Refresh</button>